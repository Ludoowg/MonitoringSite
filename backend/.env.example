# Local development without Docker — copy to backend/.env

PORT=5000
NODE_ENV=development
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/monitoringsite?schema=public
HTTP_CHECK_TIMEOUT_MS=5000
SLOW_THRESHOLD_MS=2000
