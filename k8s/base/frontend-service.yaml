apiVersion: v1
kind: Service 
metadata:
  name: frontend-service
  annotations:
    argocd.argoproj.io/sync-wave: '2'

spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80

