apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: frontend

spec:
  template:
    spec:
      containers:
      - name: frontend
        resources:
          requests: 
            cpu: "50m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"

