apiVersion: v1
kind: Service
metadata:
  name: backend-service
  annotations:
    argocd.argoproj.io/sync-wave: '2'

spec: 
  type: ClusterIP
  selector: 
    app: backend
  ports:
    - port: 3000
      targetPort: 3000
      