apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: backend

spec:
  template:
    spec:
      containers:
      - name: backend
        resources:
          requests: 
            cpu: "50m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"

